// Stub for the `server-only` package: repository adapters import it to keep
// accidental client bundles out. Under vitest there is no React server
// condition, so the real package throws on import.
export {};
